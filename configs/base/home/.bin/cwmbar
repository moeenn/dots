#! /bin/bash

SCREEN_WIDTH=1366
SCREEN_HEIGHT=768

PADDING=10
PANEL_HEIGHT=40
PANEL_WIDTH=240

X=$((SCREEN_WIDTH - PADDING - PANEL_WIDTH))
Y=$((SCREEN_HEIGHT - PADDING - PANEL_HEIGHT))

cwmstatus | dzen2 -w $PANEL_WIDTH -h $PANEL_HEIGHT -x $X -y $Y
