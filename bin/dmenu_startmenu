#! /bin/bash
actions="firefox
xfce4-terminal
chromium-browser
thunar
subl
gimp"
result=$(echo "$actions" | dmenu -l 10 -w 400 -h 30 -i -x 468)
cmd=$(echo "$result")
[ -n "$cmd" ] && eval setsid setsid "$cmd"
